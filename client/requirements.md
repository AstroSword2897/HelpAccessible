## Packages
recharts | Dashboard analytics charts and data visualization
framer-motion | Page transitions and interactive playful animations for the child UI
date-fns | Human-readable date formatting for session logs
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility to merge tailwind classes safely

## Notes
MVP assumes exactly one child profile exists (ID 1)
No authentication required for MVP
Dynamic images not required, using CSS and Lucide icons heavily for AAC interface
